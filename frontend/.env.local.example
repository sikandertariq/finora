NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
NEXT_PUBLIC_DEMO_USERNAME=demo
NEXT_PUBLIC_DEMO_PASSWORD=demo-password

# Vercel only: server-side origin for the same-origin /api proxy. Keep this
# without a trailing slash and do not expose it as NEXT_PUBLIC_.
BACKEND_ORIGIN=https://YOUR_ELASTIC_IP
